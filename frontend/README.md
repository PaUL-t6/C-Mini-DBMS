Three separate terminals required
1. go into backend folder ->  gcc src/*.c main.c -Iinclude -o dbms  -> ./dbms

2. go into middleware folder -> npm install -> npm start

3. go into frontend folder -> npm install ->npm start